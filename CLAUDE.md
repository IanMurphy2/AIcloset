## Agent skills

### Issue tracker

Issues viven en Linear via MCP (servidor `linear`). See `docs/agents/issue-tracker.md`.

### Triage labels

Usando los cinco roles canónicos por defecto. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: `CONTEXT-MAP.md` en la raíz apunta a `back/CONTEXT.md` y `front/CONTEXT.md`. See `docs/agents/domain.md`.
